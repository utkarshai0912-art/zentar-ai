You are an expert Railway deployment engineer.

The application is already complete.

DO NOT rebuild the application.

ONLY fix the Railway deployment configuration.

=========================================
CURRENT ERROR
=========================================

Railway reports:

"Your build failed because the repository at this commit contains a railway.json configured to use the Dockerfile builder, but no usable Docker image was produced. A newer commit already switches to a working Nixpacks configuration."

This indicates the project still contains old Docker-based Railway configuration that conflicts with the current deployment.

=========================================
YOUR TASK
=========================================

Inspect the entire repository and remove every Docker-based Railway configuration.

Specifically:

1. Remove or replace any `railway.json` that specifies:
   - builder: DOCKERFILE
   - dockerfilePath
   - docker build configuration

2. Remove any obsolete deployment configuration that forces Docker.

3. Verify there are no conflicting deployment files.

=========================================
USE NIXPACKS ONLY
=========================================

Configure Railway to use Nixpacks instead of Docker.

Generate or fix:

- nixpacks.toml
- railway.toml (if required)
- Procfile (if required)
- requirements.txt
- runtime.txt (if required)

Do NOT generate:

- Dockerfile
- docker-compose.yml
- railway.json configured for Docker

=========================================
FASTAPI CONFIGURATION
=========================================

Ensure the backend starts using:

- Host: 0.0.0.0
- Port: environment variable PORT
- Production-ready Uvicorn or Gunicorn configuration
- Railway-compatible startup command

=========================================
PROJECT STRUCTURE
=========================================

Automatically detect:

- Backend location
- Python entry point
- FastAPI app instance
- Correct module path
- Existing deployment files

If the backend is inside `/backend`, configure Railway to build from that directory correctly.

=========================================
VALIDATE
=========================================

Check for:

- Duplicate Railway configs
- Old Docker configs
- Invalid build commands
- Wrong start command
- Missing dependencies
- Incorrect working directory
- Health check configuration

=========================================
OUTPUT
=========================================

Provide:

1. Root cause of the deployment failure.
2. Every incorrect deployment file.
3. Every corrected deployment file.
4. Complete contents of each new file.
5. Railway Root Directory.
6. Railway Build Command.
7. Railway Start Command.
8. Deployment checklist.
9. Confirm the project deploys successfully on Railway using Nixpacks without Docker.
