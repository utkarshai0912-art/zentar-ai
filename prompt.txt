You are an expert FastAPI and Railway deployment engineer.

The project is already complete.

DO NOT rebuild the project.

DO NOT redesign the architecture.

ONLY fix the Railway deployment.

Current Status:

✔ Build succeeds
✔ Image is pushed successfully
✔ Service starts deployment
❌ Railway Health Check fails

Logs show:

Starting Healthcheck

Path: /health

Retry window: 30s

Attempt #1 failed with service unavailable

Your task is to find the root cause and permanently fix it.

=================================================
CHECK ALL POSSIBLE CAUSES
=================================================

Inspect the entire backend and verify:

1. The FastAPI server is actually starting.

2. The application binds to

host = 0.0.0.0

instead of localhost.

3. The application listens on

PORT=os.environ["PORT"]

instead of a hardcoded port.

4. Startup events are not crashing.

5. Database connection failures do not prevent server startup.

6. Redis connection failures do not prevent startup.

7. Vector database failures do not prevent startup.

8. MCP failures do not stop the API.

9. Browser initialization (Playwright/Chromium) does not block startup.

10. Background workers should start after the API becomes healthy.

=================================================
HEALTH ENDPOINT
=================================================

Create or fix

GET /health

It must return immediately.

Example:

{
  "status":"ok",
  "service":"backend",
  "version":"1.0.0"
}

The endpoint must NOT:

Connect to database

Connect to Redis

Launch browsers

Load AI models

Initialize MCP servers

Call external APIs

Only verify that the API process is alive.

=================================================
STARTUP FIX
=================================================

Move heavy initialization into background startup tasks.

Examples:

Playwright

Browser pool

Redis

Qdrant

Supabase

OpenAI

Embedding models

MCP servers

Load these AFTER the server starts accepting requests.

=================================================
ERROR HANDLING
=================================================

If any external dependency fails:

Database

Redis

Supabase

Qdrant

Playwright

Chromium

MCP

OpenAI

Gemini

Anthropic

SMTP

Do NOT crash the server.

Instead:

Log the error

Mark service unavailable internally

Continue running

=================================================
RAILWAY CONFIGURATION
=================================================

Verify:

PORT environment variable

Host = 0.0.0.0

Correct startup command

Correct working directory

Correct Procfile

Correct railway.toml

Correct requirements.txt

=================================================
VERIFY PLAYWRIGHT
=================================================

If Playwright is enabled:

Ensure Chromium exists.

If Chromium cannot launch,

disable browser service temporarily

instead of crashing the backend.

=================================================
OUTPUT
=================================================

1. Identify the exact reason the Railway health check fails.

2. Show every affected file.

3. Fix all startup issues.

4. Generate corrected code.

5. Ensure /health responds in under 100ms.

6. Ensure Railway health checks pass.

7. Ensure the backend continues running even if optional services fail.

The final backend must deploy successfully on Railway without Docker and pass the Railway health check on the first attempt.
